# v1.0.1

Patch release — minor code fixes.

## Fixes

- Removed dead code in metric unit conversion path (`convert_value`)
- Fixed f-string nested quote style for Python 3.12 compatibility in TUI status message
